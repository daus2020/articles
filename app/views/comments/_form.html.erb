
<%= form_with model: [ @article, @article.comments.build ] do |form| %>
  <p>
    <%= form.label :commenter, "Mi nombre" %><br>
    <%= form.text_field :commenter %>
  </p>
  <p>
    <%= form.label :body, "Mi comentario" %><br>
    <%= form.text_area :body %>
  </p>
  <%# <button class="btn btn-outline-primary">
    <%= form.submit %>
  <%# </button> %>
  <div class="d-flex justify-content-around">
    <%= form.submit "Enviar", class: "btn btn-outline-light text-light hover-btn mb-3 mt-2" do %>
      <i class="bi bi-cloud-arrow-up">&emsp;Enviar</i>
    <% end %>

    <button type="button" class="btn btn-outline-light text-light hover-btn mb-3 mt-2" data-bs-dismiss="modal">Cerrar</button>
  </div>

  <%# <%= link_to articles_path, class:"text-decoration-none text-light btn btn-outline-light hover-btn" do %>
    <%# <i class="bi bi-arrow-counterclockwise">&emsp;Volver a Artículos</i> %>
  <%# <% end %>

<% end %>