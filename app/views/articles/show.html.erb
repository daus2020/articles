<p style="color: green"><%= notice %></p>

<%= render @article %>

<div class="d-flex justify-content-around my-3">
<%# <div class="my-4 d-flex mx-auto"> %>
  <!-- BUTTON trigger modal to add new comment-->
  <button class="btn btn-outline-light" id="comment-btn" data-bs-toggle="modal" data-bs-target="#commentModal">
    <i class="bi bi-chat-right">&emsp;Ingrese su comentario</i>
  </button>

  <!-- BUTTON back to Articles -->
  <button class="btn btn-outline-light hover-btn" >
    <%= link_to articles_path, class:"text-decoration-none text-light" do %>
      <i class="bi bi-arrow-counterclockwise back-icon">&emsp;Volver a Artículos</i>
    <% end %>
  </button>
  <%# <button class="btn btn-outline-light back-btn" >
    <%= link_to articles_path, id:"link-btn", class:"text-decoration-none text-light" do %>
      <%# <i class="bi bi-arrow-counterclockwise back-icon">&emsp;Volver a Artículos</i> %>
    <%# <% end %>
  <%# </button> %>
  
  <%# <div id="back-btn">
    <%= link_to articles_path, class: "btn btn-outline-primary btn-sm mx-auto", title: "Volver a Artículos" do %>
      <%# <i class="bi bi-arrow-counterclockwise">&emsp;Volver a Artículos</i> %>
    <%# <% end %>
  <%# </div> %>
</div>

<h3 class="text-center text-light">Comentarios <span class="text-light fs-5">( <%= @article.comments.size %> )</></span></h3>
<br>

<% if @article.comments.size == 0 %>
  <h5 class="text-center"><%= "Sea el primero en comentar..." %></h5> 
<% else %>
  <table class="table table-success table-hover border border-light" id="table">
    <thead class="th-comments">
      <tr class="table-info">
        <th scope="col-4" class="text-left">
          <strong>Hecho por</strong>
        </th>
        <th scope="col-7" class="text-center">
          <strong>Comentario</strong>
        </th>
        <th scope="col-1" class="text-end">
          <strong>Eliminar</strong>
        </th>
      </tr>
    </thead>
    <tbody>
      <%= render @article.comments %>
    </tbody>
  </table>
<% end %>



<!-- Modal -->
<div class="modal fade" id="commentModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <%# <h5 class="modal-title" id="exampleModalLabel">Modal title</h5> %>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <%= render 'comments/form' %>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
        <button type="button" class="btn btn-primary">Save changes</button>
      </div>
    </div>
  </div>
</div>



<%# <h3 class="my-3 text-center">Ingrese su comentario:</h3> %>
<%# <%= render 'comments/form' %>